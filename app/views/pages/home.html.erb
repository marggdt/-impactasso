<div class="home_banner">
   <div class="row">
      <div class="col-lg-6">
        <div class="text">
          <h1>Impact'Asso </h1>
          <h2>Agis facilement pour des causes qui te tiennent à coeur</h2>
          <span>La solution pour trouver facilement des associations et des missions de bénévolat qui te ressemblent, autour de chez toi!</span>
        </div>
        <div class="form-group ">
          <%= form_tag assos_path, method: :get do %>
            <div class="form-group input-text">
              <%= text_field_tag :query, params[:query], class: "form-control", placeholder: "Trouver une asso" %>
              <%= submit_tag "Rechercher", class: "btn btn-primary " %>
              <% end %>
            </div>
        </div>
      </div>
      <div class="col-lg-6">
        <%= image_tag "9814.jpg", class: "img_preview" %>
      </div>
   </div>
</div>
<div class="container max-height-100vh">
  <div class="mapbox">
    <h3> Autour de moi </h3>
    <div class="map_style" id="map"
    style="width: 100%; height: 600px;"
    data-markers="<%= @markers.to_json %>"
    data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
</div>

