<div class="home_banner">
  <div class="home_text d-flex">
    <div class="text">
      <h1>Impact'Asso </h1>
        <h2>Agis facilement pour des causes qui te tiennent à coeur</h2>
          <span>Découvre des associations et des missions de bénévolat qui te ressemblent, autour de chez toi!</span>
    </div>
    <div class="boutons_home">
     <%= form_tag assos_path, method: :get do %>
      <div class="form-group">
        <%= text_field_tag :query,
          params[:query],
          class: "form-control",
          placeholder: "Trouver une asso"
        %>
      </div>
      <%= submit_tag "Rechercher", class: "btn btn-primary " %>
    <% end %>
    </div>
  </div>
</div>
<div class="container max-height-100vh">
  <div class="mapbox ">
    <h2> Les associations autour de moi </h2>
    <div class="map_style" id="map"
      style="width: 100%; height: 600px;"
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
  </div>
</div>



